// Place your settings in this file to overwrite default and user settings.
{
    "editor.tabSize": 2,
    "files.eol": "\n",
    "files.exclude": {
        "**/*.bak": true,
        "**/*.js.map": true,
        "**/*.js": {"when": "$(basename).ts"}
    }
}